<!DOCTYPE html>
<html>

<head>

  <meta charset="utf-8" />
  <title class="yay"></title>

  <link href='https://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.2.19/angular.min.js"></script>
  <script src="/javascripts/main.js"></script>


</head>

<body ng-app = "bottle">

<div class = "mainContent row" ng-controller = "MainContent as main">

  <div class = "listContainer col-xs-12 col-sm-6">
    <div class = "commentList col-xs-12" ng-repeat = "comment in main.comments">
      <div class = "commentName col-xs-12 col-sm-4">{{comment.name}}</div>
      <div class = "commentContent col-xs-12 col-sm-6">{{comment.comment}}</div>
      <div class = "delContainer col-xs-12 col-sm-2">
        <input class = "btn inputBtn delBtn" type = "submit" value = "Delete" commentid = "{{comment._id}}">
      </div>
    </div>
  </div>

  <form class = "commentForm col-xs-12 col-sm-6" name = "commentForm" ng-controller = "CommentController as commentCtrl" ng-submit = "commentForm.$valid && commentCtrl.addComment()" novalidate>
    <h3 class = "formHeader">Put your message here:</h3>

    <blockquote class = "liveView">
      <div class = "liveName">{{commentCtrl.comment.name}}</div>
      <div class = "liveComment">{{commentCtrl.comment.comment}}</div>
    </blockquote>


    <input placeholder = "Name" class = "userInput form-control" ng-model = "commentCtrl.comment.name" ng-maxlength = 18 required>
    <input placeholder = "Message" class = "userInput form-control" ng-model = "commentCtrl.comment.comment" ng-maxlength = 60 required>
    <input class = "btn inputBtn submitBtn" type = "submit" value = "Submit Comment">
  </form>
</div>


</body>

</html>